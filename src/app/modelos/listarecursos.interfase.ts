export interface IListaRecursos {
    recursosId: string;
    articulo: string;
    numero_Locker: number;
    cantidad: number;
    descripcion: string;
    categoria?: string;
    activo: boolean;
}
